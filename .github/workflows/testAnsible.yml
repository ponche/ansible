name: Workflow test 

on: 
  push:
  repository_dispatch:

jobs: 
  labo:
    name: experiment ansible version
    run-on: ubuntu-latest
    steps: 
      - name: print version ansible 
        run: ansible --versionb

      - name: print version ansible-playbook 
        run: ansible-playbook --version

  
